[package]
name = "crosshook-core"
version = "0.1.0"
edition = "2021"

[dependencies]
directories = "5"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
tokio = { version = "1", features = ["fs", "process", "sync"] }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
