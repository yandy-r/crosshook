# Quick Review — Uncommitted Changes

**Reviewed**: 2026-04-23T10:38:46-04:00
**Mode**: Quick (inline, parallel)
**Author**: local
**Branch**: feat/ui-hero-detail-phase5
**Decision**: COMMENT

## Summary

The UI refactor is mostly presentation-focused, with no security issues found. The main merge-blocking risk is commit hygiene: tracked files now depend on a new untracked helper module, so a partial commit would break the frontend build.

## Findings

### CRITICAL

None.

### HIGH

- **[F001]** `src/crosshook-native/src/components/library/HeroDetailPanels.tsx:11` — Tracked code imports `@/utils/launchPreviewPresentation`, and `src/crosshook-native/src/components/launch-panel/helpers.tsx:9` imports the same new helper via a relative path, but `src/crosshook-native/src/utils/launchPreviewPresentation.ts` is currently untracked. If only tracked changes are committed, typecheck/build fails with a missing module. `[correctness, security, quality]`
  - **Status**: Fixed
  - **Category**: Correctness
  - **Suggested fix**: Add `src/crosshook-native/src/utils/launchPreviewPresentation.ts` to the same commit, or keep the shared helper code in an already tracked module.

### MEDIUM

- **[F002]** `src/crosshook-native/src/components/library/HeroDetailPanels.tsx:84` — `LaunchPreviewStructuredView` is a new large render function with many independent sections and conditionals, exceeding the quick-review function-size threshold and making future preview changes harder to review safely. `[correctness]`
  - **Status**: Fixed
  - **Category**: Code Quality
  - **Suggested fix**: Split it into focused section components such as `LaunchPreviewSummary`, `LaunchPreviewValidation`, `LaunchPreviewCommandChain`, `LaunchPreviewProtonSetup`, `LaunchPreviewTrainer`, and `LaunchPreviewEnvironment`.

- **[F003]** `src/crosshook-native/src/components/library/HeroDetailPanels.tsx:84` — The new structured launch preview path adds behavior for summary, validation, command chain, Proton setup, trainer, environment groups, cleared variables, and raw preview without test coverage. `[quality]`
  - **Status**: Fixed
  - **Category**: Best Practices
  - **Suggested fix**: Add a focused `HeroDetailPanels` test, or extend the relevant game-detail test, with a representative `LaunchPreview` covering the new sections, grouped env output, validation issues, and empty states.

- **[F004]** `src/crosshook-native/src/styles/library.css:1071` — Feature-specific detail-panel styles are being added to already-large global stylesheets (`library.css` and `theme.css`), continuing file-size and ownership drift. `[correctness]`
  - **Status**: Fixed
  - **Category**: Code Quality
  - **Suggested fix**: Move hero-detail and collections-sidebar-specific selectors into smaller scoped stylesheets when practical, then import them from the relevant feature entry point.

### LOW

- **[F005]** `src/crosshook-native/src/utils/launchPreviewPresentation.ts:3` — New exported presentation helpers do not document their display-label contract despite being shared by multiple components. `[correctness]`
  - **Status**: Fixed
  - **Category**: Code Quality
  - **Suggested fix**: Add brief JSDoc to the exported helpers, or keep helpers module-private unless they are intentionally shared.

## Files Reviewed

- `src/crosshook-native/src/components/launch-panel/helpers.tsx` (Modified)
- `src/crosshook-native/src/components/library/GameDetailsCompatibilitySection.tsx` (Modified)
- `src/crosshook-native/src/components/library/GameDetailsHealthSection.tsx` (Modified)
- `src/crosshook-native/src/components/library/GameDetailsMetadataSection.tsx` (Modified)
- `src/crosshook-native/src/components/library/HeroDetailPanels.tsx` (Modified)
- `src/crosshook-native/src/styles/library.css` (Modified)
- `src/crosshook-native/src/styles/theme.css` (Modified)
- `src/crosshook-native/src/utils/launchPreviewPresentation.ts` (Added, untracked)
