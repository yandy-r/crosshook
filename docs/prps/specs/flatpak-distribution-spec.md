# Feature Spec: Flatpak Distribution Target

> **Authoritative doc**: Treat [`docs/prps/prds/flatpak-distribution.prd.md`](../prds/flatpak-distribution.prd.md) as the source of truth for Flatpak distribution decisions. This spec was last reconciled with the implementation on **2026-04-11** (partial sync — see TODO below).
>
> **TODO — sections still drifting from code or PRD** (reconcile when editing):
>
> - **§2 Existing State** — claims no committed manifest; repo now has `packaging/flatpak/*.yml`, desktop, metainfo.
> - **§3.3 Sandbox / §3.7 Manifest Structure** — examples should mirror `packaging/flatpak/dev.crosshook.CrossHook.yml` (done partially in §3.3 refresh).
> - **§3.5 Process Execution Strategy** — updated 2026-04-11 for `FLATPAK_ID` / `host_command()`; re-verify if spawn strategy changes.
> - **§4 Required Artifacts** — checklist vs what is already committed (`release.yml` Flatpak job may not exist yet).
> - **§7 Phase 2** — CI job not present in repo yet; when added, align the `flatpak-github-actions` image with **`runtime-version: "50"`** (see manifest).

**Issue**: [#69](https://github.com/yandy-r/crosshook/issues/69)
**Priority**: P3 (Consider Later) | **Effort**: High | **Impact**: Medium
**Status**: Research complete, ready for planning
**Date**: 2026-04-10

---

## 1. Problem Statement

CrossHook is distributed exclusively as an AppImage. While AppImage is correct for CrossHook's deep filesystem access needs, this limits discoverability and creates friction for users on immutable Linux distributions (Fedora Silverblue/Kinoite, SteamOS, NixOS, Vanilla OS) where Flatpak is the primary software installation mechanism. A Flatpak target would:

- Improve discoverability via Flathub (the largest Linux app store)
- Provide native support for immutable distro users who may lack FUSE (required by AppImage)
- Enable automatic updates through Flatpak infrastructure
- Align with the distribution model used by peer tools (Lutris, Bottles, Heroic, ProtonUp-Qt are all on Flathub)

AppImage remains the primary distribution format. Flatpak is a secondary target.

---

## 2. Existing State

### 2.1 What Already Exists

The previous `flatpak build-init` staging tree (`packaging/flatpak/build-dir/`) and local Flatpak repository (`packaging/flatpak/repo/`) were **removed** in [GH-195](https://github.com/yandy-r/crosshook/issues/195). They were not reproducible build definitions (JSON build output vs a committed YAML manifest). Phase 1 work adds committed source artifacts under `packaging/flatpak/` instead.

No Flatpak YAML manifest, desktop file, or metainfo is committed yet. Local `flatpak-builder` cache at `.flatpak-builder/` is listed in `.gitignore`.

### 2.2 Current Build Pipeline

- **Primary format**: AppImage only (`tauri.conf.json` → `bundle.targets: ["appimage"]`)
- **CI**: `.github/workflows/release.yml` — triggered by `v*` tag push, builds AppImage + CLI tarball, publishes to GitHub Releases
- **App ID**: `dev.crosshook.CrossHook` (used in Tauri config and existing Flatpak prototype)

### 2.3 Codebase Flatpak Readiness

| Area                     | Status     | Notes                                                                                                        |
| ------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------ |
| XDG paths                | Ready      | `directories::BaseDirs` handles all path resolution; Flatpak remaps automatically to `~/.var/app/<id>/`      |
| Steam Flatpak discovery  | Ready      | `steam/discovery.rs` already probes `~/.var/app/com.valvesoftware.Steam/data/Steam`                          |
| SQLite                   | Ready      | `rusqlite` with `features = ["bundled"]` — no system dependency                                              |
| TLS                      | Ready      | `reqwest` uses `rustls` — no system OpenSSL dependency                                                       |
| Icon pipeline            | Ready      | SVG source → `generate-assets.sh` → PNGs at multiple sizes                                                   |
| Desktop entry            | Partial    | Generated by `scripts/generate-crosshook-desktop.sh` but not committed as a static file                      |
| MetaInfo/AppStream       | Partial    | Prototype exists but missing Flathub-required fields                                                         |
| Resource path resolution | Needs work | `paths.rs` uses `BaseDirectory::Resource` — may not resolve to `/app/resources/` in Flatpak without override |
| Process execution        | Needs work | All `tokio::process::Command` calls spawn host binaries directly; Flatpak requires `flatpak-spawn --host`    |

---

## 3. Technical Design

### 3.1 App ID Decision

| Option | ID | Pros | Cons |
| ------ | -- | ---- | ---- |
| Legacy (pre-[#196](https://github.com/yandy-r/crosshook/issues/196)) | `com.crosshook.native` | Matched early prototype / old Tauri `identifier` | Requires `crosshook.com` domain ownership for Flathub |
| **Adopted** | `dev.crosshook.CrossHook` | Flathub-compliant GitHub reverse-DNS; matches current Tauri `identifier` | — |

**Status**: Tauri `identifier` and packaging use **`dev.crosshook.CrossHook`** ([GH-196](https://github.com/yandy-r/crosshook/issues/196)). A one-time startup migration moves legacy `com.crosshook.native` XDG app roots to the new name when the destination is empty.

### 3.2 Distribution Strategy (Two Phases)

**Phase 1: Self-hosted Flatpak** (pre-built binary approach)

- Build the Tauri app normally (produces binary + resources)
- Package the pre-built binary into a Flatpak using `flatpak-builder`
- Publish `.flatpak` bundle as a GitHub Release artifact alongside the AppImage
- Users install via `flatpak install --user crosshook.flatpak`

**Phase 2: Flathub submission** (build-from-source, future)

- Requires `flatpak-cargo-generator.py` for offline Cargo dependencies
- Requires `flatpak-node-generator` for offline npm dependencies
- Manifest lives in `flathub/dev.crosshook.CrossHook` repo
- Significantly more CI complexity — defer until demand justifies it

### 3.3 Sandbox Permissions

Based on comprehensive filesystem/process/network access analysis:

```yaml
finish-args:
  # Display
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri

  # Audio (games need audio via Wine/Proton)
  - --socket=pulseaudio

  # Network (ProtonDB, Steam metadata, SteamGridDB, GE-Proton downloads, git clone for taps)
  - --share=network

  # Host command execution (Proton, Wine, Steam, winetricks, protontricks, gamescope, mangohud)
  - --talk-name=org.freedesktop.Flatpak

  # WebKitGTK NVIDIA workaround (de facto standard for WebKitGTK Flatpak apps)
  - --env=WEBKIT_DISABLE_DMABUF_RENDERER=1

  # Flatpak detection: `flatpak run` sets FLATPAK_ID (app id). The manifest does not set
  # FLATPAK=1. Rust checks FLATPAK_ID and /.flatpak-info — see crosshook-core/src/platform.rs.
  # (No finish-args env line required for detection.)

  # Filesystem — match packaging/flatpak/dev.crosshook.CrossHook.yml (not --filesystem=host)
  - --filesystem=home
  - --filesystem=/mnt
  - --filesystem=/run/media
  - --filesystem=/media
  - --filesystem=~/.var/app/com.valvesoftware.Steam:ro
```

### 3.4 Filesystem Access Justification

CrossHook requires broad path visibility (the committed manifest uses `--filesystem=home` plus `/mnt`, `/run/media`, `/media`, and read-only Flatpak Steam — not full `--filesystem=host`). This is not a convenience shortcut — it is architecturally required because:

1. **Steam library folders** can be on any mounted drive (`/mnt/games`, `/run/media/user/SSD`, etc.) — paths are discovered at runtime from `libraryfolders.vdf`
2. **Proton/Wine binaries** live inside Steam library folders at arbitrary locations
3. **Wine prefixes (compatdata)** are inside Steam library folders
4. **Trainer executables** are user-specified paths — can be anywhere on the filesystem
5. **ProtonUp writes** to `compatibilitytools.d/` inside Steam's directory tree
6. **Runtime helper scripts** need to execute `steam`, `pgrep`, and other host binaries
7. **External tools** (`winetricks`, `protontricks`, `mangohud`, `gamescope`, `umu-run`) are host-installed at arbitrary PATH locations

**Precedent**: Lutris (accepted on Flathub) uses `--filesystem=home` with `--talk-name=org.freedesktop.Flatpak` for equivalent reasons. CrossHook's committed manifest uses `home` plus `/mnt`, `/run/media`, `/media`, and read-only Flatpak Steam (see `packaging/flatpak/dev.crosshook.CrossHook.yml`).

### 3.5 Process Execution Strategy

CrossHook spawns host processes extensively. Inside the Flatpak sandbox, host `/usr` and tools are masked; **`flatpak-spawn --host`** (via `host_command()` in `crosshook-core/src/platform.rs` when `is_flatpak()` is true) reaches the host environment. Detection uses **`FLATPAK_ID`** (set by `flatpak run`) and **`/.flatpak-info`**, not `FLATPAK=1`.

| Approach                           | Mechanism                                                                           | Complexity | Compatibility                                             |
| ---------------------------------- | ----------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------- |
| A: Targeted `--filesystem` mounts  | Grants paths for Steam libraries, removable media, etc. (current manifest)          | Medium     | Host tools still need `flatpak-spawn --host` where masked |
| B: `flatpak-spawn --host` wrapper  | Core wraps host spawns when `is_flatpak()` is true (`FLATPAK_ID` / `/.flatpak-info`) | Medium     | Full host access including `/proc` for `pgrep` where needed |

**Recommendation**: Keep the manifest’s targeted filesystem permissions; rely on `host_command()` / `flatpak-spawn --host` for host binaries. If specific failures appear during testing, adjust helpers or spawns selectively.

**Implementation detail** (`crosshook-core/src/platform.rs`):

```rust
/// Returns true when running inside a Flatpak sandbox.
pub fn is_flatpak() -> bool {
    std::env::var_os("FLATPAK_ID").is_some()
        || std::path::Path::new("/.flatpak-info").exists()
}
```

### 3.6 Resource Path Resolution

Tauri's `BaseDirectory::Resource` resolves to `/usr/lib/<app>/` on native Linux installs, but Flatpak installs to `/app/`. The existing `paths.rs` already has a fallback chain. For Flatpak:

- The manifest installs resources to `/app/resources/`
- Tauri's resolver may not find `/app/resources/` automatically
- Add a Flatpak-aware path to the resolution chain in `resolve_bundled_script_path()`:

```rust
fn resolve_bundled_script_path(app: &tauri::AppHandle, script_name: &str) -> Option<PathBuf> {
    use tauri::path::BaseDirectory;
    use tauri::Manager;

    // Standard Tauri resource resolution
    if let Ok(path) = app.path().resolve(script_name, BaseDirectory::Resource) {
        if path.exists() {
            return Some(path);
        }
    }

    // Flatpak: resources installed to /app/resources/
    let flatpak_path = PathBuf::from("/app/resources").join(script_name);
    if flatpak_path.exists() {
        return Some(flatpak_path);
    }

    None
}
```

### 3.7 Manifest Structure (Phase 1)

```yaml
# dev.crosshook.CrossHook.yml (abbreviated — see repo for full modules/sources)
app-id: dev.crosshook.CrossHook
runtime: org.gnome.Platform
runtime-version: "50"
sdk: org.gnome.Sdk
command: crosshook-native

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri
  - --socket=pulseaudio
  - --share=network
  - --talk-name=org.freedesktop.Flatpak
  - --env=WEBKIT_DISABLE_DMABUF_RENDERER=1
  - --filesystem=home
  - --filesystem=/mnt
  - --filesystem=/run/media
  - --filesystem=/media
  - --filesystem=~/.var/app/com.valvesoftware.Steam:ro

modules:
  - name: crosshook
    buildsystem: simple
    sources:
      - type: dir
        path: staging
    build-commands:
      - install -Dm755 crosshook-native /app/bin/crosshook-native
      - mkdir -p /app/resources
      - install -Dm755 runtime-helpers/*.sh /app/resources/
      - install -Dm644 icon-512.png /app/share/icons/hicolor/512x512/apps/dev.crosshook.CrossHook.png
      - install -Dm644 icon-256.png /app/share/icons/hicolor/256x256/apps/dev.crosshook.CrossHook.png
      - install -Dm644 icon-128.png /app/share/icons/hicolor/128x128/apps/dev.crosshook.CrossHook.png
      - install -Dm644 dev.crosshook.CrossHook.desktop /app/share/applications/dev.crosshook.CrossHook.desktop
      - install -Dm644 dev.crosshook.CrossHook.metainfo.xml /app/share/metainfo/dev.crosshook.CrossHook.metainfo.xml
```

---

## 4. Required Artifacts

### 4.1 New Files

| File                                                  | Purpose                                                                | Location                       |
| ----------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------ |
| `packaging/flatpak/dev.crosshook.CrossHook.yml`          | Flatpak manifest (committed, not build-dir staging)                    | Top-level of Flatpak packaging |
| `packaging/flatpak/dev.crosshook.CrossHook.desktop`      | Static desktop entry                                                   | Committed source-of-truth      |
| `packaging/flatpak/dev.crosshook.CrossHook.metainfo.xml` | AppStream metadata (Flathub-complete)                                  | Committed source-of-truth      |
| `scripts/build-flatpak.sh`                            | Build script that stages binary + resources and runs `flatpak-builder` | Build tooling                  |
| `.github/workflows/release.yml` (modify)              | Add Flatpak build job alongside existing AppImage job                  | CI                             |

### 4.2 MetaInfo Requirements

The existing prototype is missing required fields. Complete metainfo must include:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>dev.crosshook.CrossHook</id>
  <metadata_license>MIT</metadata_license>
  <project_license>MIT</project_license>
  <name>CrossHook</name>
  <summary>Steam and Proton trainer orchestration for Linux</summary>

  <developer id="com.github.yandy-r">
    <name>Yandy Ramirez</name>
  </developer>

  <description>
    <p>CrossHook helps you launch Windows game trainers under Proton on Linux.
       Configure launch profiles, manage Proton versions, orchestrate trainer
       injection timing, and monitor launch health — all from a native desktop UI.</p>
    <p>Supports Steam library auto-detection, GE-Proton installation, prefix
       dependency management, community profile sharing, and ProtonDB integration.</p>
  </description>

  <url type="homepage">https://github.com/yandy-r/crosshook</url>
  <url type="bugtracker">https://github.com/yandy-r/crosshook/issues</url>
  <url type="vcs-browser">https://github.com/yandy-r/crosshook</url>

  <launchable type="desktop-id">dev.crosshook.CrossHook.desktop</launchable>

  <screenshots>
    <!-- Required: at least one screenshot with caption -->
    <screenshot type="default">
      <caption>Profile editor with launch configuration</caption>
      <image>https://raw.githubusercontent.com/yandy-r/crosshook/main/docs/images/screenshot-profile-editor.png</image>
    </screenshot>
  </screenshots>

  <content_rating type="oars-1.1"/>

  <releases>
    <release version="0.2.9" date="YYYY-MM-DD">
      <description><p>Release notes here.</p></description>
    </release>
  </releases>

  <branding>
    <color type="primary" scheme_preference="light">#ffffff</color>
    <color type="primary" scheme_preference="dark">#1a1a2e</color>
  </branding>
</component>
```

### 4.3 Code Changes

| Change                                                           | File                                             | Scope                                                   |
| ---------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------- |
| Add `is_flatpak()` detection                                     | `crosshook-core/src/lib.rs` or new `platform.rs` | Small — one function                                    |
| Add Flatpak resource path fallback                               | `src-tauri/src/paths.rs`                         | Small — add one branch to `resolve_bundled_script_path` |
| Conditionally wrap process execution with `flatpak-spawn --host` | `crosshook-core/src/launch/runtime_helpers.rs`   | Medium — only if direct execution fails in testing      |
| Handle `pgrep` inside Flatpak                                    | Runtime helper scripts                           | Small — may need `flatpak-spawn --host pgrep`           |

---

## 5. Risks and Mitigations

### 5.1 High Risk

| Risk                                                                                                                                                  | Impact                                      | Mitigation                                                                                                                                             |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Process execution fails in sandbox** — helper scripts call `steam`, Proton, `pgrep`, `unshare` which may not be visible despite filesystem mounts | Core launch workflow broken                 | Test early with `flatpak run --command=bash dev.crosshook.CrossHook` to verify binary visibility. Use `flatpak-spawn --host` via `host_command()` where needed. |
| **Flathub rejects broad filesystem permissions**                                                                                                               | Cannot publish to Flathub                   | Maintain self-hosted distribution (GitHub Releases). The current manifest already avoids full `--filesystem=host`; negotiate further tightening with reviewers if required.                                       |
| **`unshare --user --net` blocked by Flatpak seccomp** — trainer network isolation relies on user namespaces                                           | Network isolation feature broken in Flatpak | Detect Flatpak and gracefully degrade — skip network isolation with a user-visible warning. The `is_unshare_net_available()` probe should catch this.  |

### 5.2 Medium Risk

| Risk                                                                                                   | Impact                                                  | Mitigation                                                                                             |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| **Tauri resource paths don't resolve in `/app/`**                                                      | Bundled scripts not found → launch fails                | Add explicit `/app/resources/` fallback in `resolve_bundled_script_path()`                             |
| **WebKitGTK rendering issues on NVIDIA**                                                               | Blank screen or flickering                              | `WEBKIT_DISABLE_DMABUF_RENDERER=1` in `finish-args` (matches Lutris approach)                          |
| **Steam Flatpak interop** — reading `~/.var/app/com.valvesoftware.Steam/` when both apps are sandboxed | Cannot discover Steam libraries for Flatpak Steam users | Manifest includes `--filesystem=~/.var/app/com.valvesoftware.Steam:ro` |

### 5.3 Low Risk

| Risk                                                 | Impact                              | Mitigation                                                              |
| ---------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------- |
| **Build time increase** — Flatpak build adds CI time | Longer releases                     | Run Flatpak build in parallel with AppImage build in CI                 |
| **XDG path remapping confusion**                     | Config at unexpected path for users | Document that Flatpak stores data at `~/.var/app/dev.crosshook.CrossHook/` |

---

## 6. Testing Strategy

### 6.1 Manual Verification Matrix

| Test Case                                        | Expected Result                                                 | Priority |
| ------------------------------------------------ | --------------------------------------------------------------- | -------- |
| Install Flatpak bundle and launch app            | App window opens, no blank screen                               | P0       |
| Steam library auto-detection (native Steam)      | Discovers `~/.local/share/Steam`                                | P0       |
| Steam library auto-detection (Flatpak Steam)     | Discovers `~/.var/app/com.valvesoftware.Steam/data/Steam`       | P0       |
| Create profile and launch game via Proton        | Game launches, trainer launches after delay                     | P0       |
| ProtonDB integration                             | Ratings load from network                                       | P1       |
| GE-Proton download and install                   | Archive downloads and extracts to correct path                  | P1       |
| Community tap clone                              | `git clone` succeeds from within sandbox                        | P1       |
| Trainer network isolation (`unshare`)            | Either works or degrades gracefully with warning                | P1       |
| `pgrep` game process detection in helper scripts | Process detected, trainer timing works                          | P0       |
| Settings persistence across app restarts         | Settings at `~/.var/app/dev.crosshook.CrossHook/config/crosshook/` | P1       |
| SQLite metadata DB persistence                   | DB at `~/.var/app/dev.crosshook.CrossHook/data/crosshook/`         | P1       |
| Game image cache                                 | Images cached and displayed via asset protocol                  | P2       |
| NVIDIA GPU with Wayland                          | No blank screen (DMABUF workaround active)                      | P2       |

### 6.2 Automated CI Validation

- Build the Flatpak bundle in CI and upload as artifact
- Validate MetaInfo with `appstreamcli validate dev.crosshook.CrossHook.metainfo.xml`
- Validate desktop file with `desktop-file-validate dev.crosshook.CrossHook.desktop`
- Verify bundle installs without errors: `flatpak install --user <bundle>`

---

## 7. Implementation Phases

### Phase 1: Flatpak Build Infrastructure (MVP)

**Goal**: Produce a working `.flatpak` bundle from the existing pre-built binary.

1. Add committed manifest, desktop entry, metainfo, and helper assets under `packaging/flatpak/` (prototype directories already removed; see §2.1)
2. Create `scripts/build-flatpak.sh` that:
   - Runs the normal Tauri build (or accepts a pre-built binary)
   - Stages binary, runtime helpers, icon, desktop file, metainfo into a staging dir
   - Runs `flatpak-builder` with the committed manifest
   - Produces a `.flatpak` bundle
3. Add `is_flatpak()` detection to `crosshook-core`
4. Add `/app/resources/` fallback to `paths.rs`
5. Complete the MetaInfo XML with all required fields
6. Test the core launch workflow inside the sandbox

### Phase 2: CI Integration

**Goal**: Automate Flatpak builds in the release pipeline.

1. Add a `flatpak` job to `release.yml` running in parallel with the AppImage job
2. Use `flatpak/flatpak-github-actions/flatpak-builder@v6` with a GNOME **50**-compatible image (align with `runtime-version: "50"` in the manifest when wiring CI)
3. Upload `.flatpak` bundle as a GitHub Release artifact
4. Add MetaInfo and desktop file validation steps

### Phase 3: Process Execution Hardening

**Goal**: Ensure all process execution works reliably within the sandbox.

1. Test each external binary: `steam`, Proton, `pgrep`, `unshare`, `winetricks`, `protontricks`, `mangohud`, `gamescope`, `umu-run`, `git`, `getent`
2. Where direct execution fails, wrap with `flatpak-spawn --host`
3. Implement graceful degradation for `unshare --user --net` (already partially handled by `is_unshare_net_available()`)
4. Update helper scripts to detect Flatpak environment and adjust `pgrep` calls if needed

### Phase 4: Flathub Submission (Future)

**Goal**: Publish to Flathub for discoverability.

1. Resolve app ID (domain ownership vs `io.github.` prefix)
2. Create screenshots for MetaInfo
3. Generate OARS content rating via https://hughsie.github.io/oars/
4. Fork `flathub/flathub`, create manifest PR targeting `new-pr` branch
5. Respond to reviewer feedback (expect multiple rounds)
6. If `--filesystem=host` is rejected, prepare argument using Lutris precedent or negotiate tighter permission set

---

## 8. Comparable Projects on Flathub

| Project                   | Runtime           | Filesystem Permissions                                      | Host Execution                                 | Lessons for CrossHook                                                                                                  |
| ------------------------- | ----------------- | ----------------------------------------------------------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **Lutris**                | GNOME 49          | `home`, `/run/media`, `/media`, Flatpak Steam path          | `--talk-name=org.freedesktop.Flatpak`          | Closest precedent — proves game tools with broad FS access are accepted. Also sets `WEBKIT_DISABLE_DMABUF_RENDERER=1`. |
| **Heroic**                | freedesktop 24.08 | Targeted paths (`~/Games/Heroic`, `~/.steam`, mount points) | No host execution — uses internal Wine Manager | Shows tighter permissions are possible but requires bundling Wine/Proton. Not viable for CrossHook.                    |
| **ProtonUp-Qt**           | KDE 6.8           | Per-tool paths (Steam, Lutris, Bottles, Heroic)             | `--talk-name=org.freedesktop.Flatpak`          | Shows targeted paths work when you know all target locations. CrossHook's paths are more dynamic.                      |
| **Bottles**               | GNOME 48          | None (portals only)                                         | Internal Wine management                       | Maximum sandboxing but requires reimplementing all file access via portals. Not practical for CrossHook's workflow.    |
| **Pomodorolm** (Tauri v2) | GNOME 46          | Minimal                                                     | None                                           | Only confirmed Tauri v2 app through Flathub review. Documented `/app/` resource path workaround.                       |

---

## 9. Open Questions

1. **Domain ownership**: Does the project control `crosshook.com`? If not, Flathub submission uses the GitHub-based app ID `dev.crosshook.CrossHook` (see [#196](https://github.com/yandy-r/crosshook/issues/196)).
2. **Screenshot availability**: Flathub requires screenshots in the MetaInfo. Do publishable screenshots exist, or do they need to be created?
3. **GNOME runtime version pinning**: The manifest uses **`runtime-version: "50"`** (`packaging/flatpak/dev.crosshook.CrossHook.yml`). Bump deliberately when a new stable is validated (WebKitGTK, SDK deps).
4. **`flatpak-spawn` latency**: The D-Bus round-trip for `flatpak-spawn --host` adds overhead to process launches. Is the trainer launch timing (delay between game and trainer start) sensitive enough that this matters?
5. **Self-hosted Flatpak repo**: Should CrossHook maintain a Flatpak repo (via `flat-manager`) for updates, or just ship `.flatpak` bundles on GitHub Releases?
6. **`build-dir/` cleanup**: **Resolved (#195)** — prototype `build-dir/` and `repo/` were removed; new work uses committed manifests only.

---

## 10. References

### Internal

- Issue: [#69 — Flatpak distribution target](https://github.com/yandy-r/crosshook/issues/69)
- Research: `docs/research/additional-features/deep-research-report.md` (P3 classification)
- Implementation guide: `docs/research/additional-features/implementation-guide.md` (Phase 7, trigger: immutable distro user reports)
- Prototype cleanup: `packaging/flatpak/build-dir/` and `packaging/flatpak/repo/` removed in [#195](https://github.com/yandy-r/crosshook/issues/195)

### External

- [Tauri v2 Flatpak Distribution Guide](https://github.com/tauri-apps/tauri-docs/blob/v2/src/content/docs/distribute/flatpak.mdx)
- [Packaging Tauri v2 for Flatpak/Flathub (Pomodorolm)](https://vincent.jousse.org/blog/en/packaging-tauri-v2-flatpak-snapcraft-elm/)
- [Flathub Submission Requirements](https://docs.flathub.org/docs/for-app-authors/requirements)
- [Flathub MetaInfo Guidelines](https://docs.flathub.org/docs/for-app-authors/metainfo-guidelines)
- [Flatpak GitHub Actions](https://github.com/flatpak/flatpak-github-actions)
- [Flatpak Cargo Generator](https://github.com/flatpak/flatpak-builder-tools/tree/master/cargo)
- [Lutris Flathub Manifest](https://github.com/flathub/net.lutris.Lutris/blob/beta/net.lutris.Lutris.yml)
- [Heroic Flathub Manifest](https://github.com/flathub/com.heroicgameslauncher.hgl)
- [ProtonUp-Qt Flathub Manifest](https://github.com/flathub/net.davidotek.pupgui2)
- [Bottles Flathub Manifest](https://github.com/flathub/com.usebottles.bottles)
- [Tauri WebKitGTK NVIDIA Issues](https://github.com/tauri-apps/tauri/issues/9394)
- [flatpak-spawn man page](https://man7.org/linux/man-pages/man1/flatpak-spawn.1.html)
